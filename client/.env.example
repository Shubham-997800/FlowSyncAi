# Base URL of the FlowSync backend API.
# Optional - defaults to http://localhost:5000 in dev (via Vite proxy) and
# relative /api (same origin) in production. Set only to override.
# VITE_API_URL=http://localhost:5000
